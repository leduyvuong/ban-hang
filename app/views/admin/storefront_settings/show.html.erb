<div class="space-y-10">
  <% if @available_shops.present? %>
    <section class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
      <h2 class="text-lg font-semibold text-slate-900">Chọn cửa hàng cần cấu hình</h2>
      <p class="mt-2 text-sm text-slate-500">Lựa chọn cửa hàng để cập nhật giao diện trang chủ.</p>
      <%= form_with url: admin_storefront_settings_path, method: :get, local: true, class: "mt-4" do |form| %>
        <div class="flex flex-col gap-4 sm:flex-row sm:items-center">
          <%= form.label :shop_id, "Cửa hàng", class: "text-sm font-medium text-slate-700" %>
          <%= form.select :shop_id, options_from_collection_for_select(@available_shops, :id, :name, @shop&.id), {}, class: "w-full rounded-xl border border-slate-200 bg-white px-4 py-2 text-sm text-slate-700 shadow-sm transition focus:border-indigo-500 focus:outline-none focus:ring-2 focus:ring-indigo-100 sm:w-80", onchange: "this.form.requestSubmit()" %>
        </div>
      <% end %>
    </section>
  <% end %>

  <% classic_selected = @shop.homepage_variant.to_s == "classic" %>
  <% modern_selected = @shop.homepage_variant.to_s == "modern" %>
  <% template_three_selected = @shop.homepage_variant.to_s == "template_3" %>

  <section class="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">
    <div class="flex flex-col gap-6 md:flex-row md:items-center md:justify-between">
      <div>
        <h2 class="text-2xl font-semibold text-slate-900">Cài đặt trang chủ</h2>
        <p class="mt-2 text-sm text-slate-500">Chọn giao diện hiển thị cho khách hàng khi truy cập trang chủ BanHang.</p>
      </div>
      <div class="rounded-full bg-indigo-50 px-4 py-2 text-sm font-semibold text-indigo-600">Hiện tại: <%= @shop.homepage_variant.to_s.titleize %></div>
    </div>

    <%= form_with model: @shop, url: admin_storefront_settings_path, method: :patch, local: true, class: "mt-8 space-y-8" do |form| %>
      <%= hidden_field_tag :shop_id, @shop.id if can_switch_between_shops? %>
      <div class="grid grid-cols-1 gap-6 lg:grid-cols-3">
        <label class="group flex h-full cursor-pointer flex-col justify-between rounded-3xl border-2 bg-white p-6 transition-all duration-300 hover:border-indigo-400 hover:shadow-lg <%= 'border-indigo-500 shadow-lg' if classic_selected %>">
          <div class="space-y-5">
            <div class="flex items-center justify-between">
              <span class="inline-flex items-center gap-2 text-base font-semibold text-slate-900">
                <span class="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-slate-100 text-slate-600">C</span>
                Classic homepage
              </span>
              <%= form.radio_button :homepage_variant, :classic, class: "h-4 w-4 border-slate-300 text-indigo-600 focus:ring-indigo-500" %>
            </div>
            <p class="text-sm text-slate-500">Giao diện hiện tại với hero banner, danh sách sản phẩm nổi bật và các danh mục nổi bật.</p>
            <ul class="space-y-2 text-sm text-slate-600">
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-slate-100 text-xs font-semibold text-slate-500">1</span> Bố cục quen thuộc, tải nhanh.</li>
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-slate-100 text-xs font-semibold text-slate-500">2</span> Phù hợp khi chưa cần nội dung marketing chuyên sâu.</li>
            </ul>
          </div>
          <div class="mt-6 rounded-2xl border border-slate-200 bg-slate-50 p-4 text-center text-xs font-medium uppercase tracking-wide text-slate-500">Layout cổ điển</div>
        </label>

        <label class="group flex h-full cursor-pointer flex-col justify-between rounded-3xl border-2 bg-white p-6 transition-all duration-300 hover:border-indigo-400 hover:shadow-lg <%= 'border-indigo-500 shadow-lg' if modern_selected %>">
          <div class="space-y-5">
            <div class="flex items-center justify-between">
              <span class="inline-flex items-center gap-2 text-base font-semibold text-slate-900">
                <span class="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-blue-100 text-blue-600">M</span>
                Modern homepage
              </span>
              <%= form.radio_button :homepage_variant, :modern, class: "h-4 w-4 border-slate-300 text-indigo-600 focus:ring-indigo-500" %>
            </div>
            <p class="text-sm text-slate-500">Giao diện mới với hero gradient, testimonial, lưới sản phẩm động và CTA nổi bật.</p>
            <ul class="space-y-2 text-sm text-slate-600">
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-blue-100 text-xs font-semibold text-blue-600">1</span> Điều hướng sticky với menu và tìm kiếm nâng cao.</li>
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-blue-100 text-xs font-semibold text-blue-600">2</span> Các section marketing: Features, Testimonials, CTA và Footer mới.</li>
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-blue-100 text-xs font-semibold text-blue-600">3</span> Hiệu ứng animation mượt mà bằng Intersection Observer.</li>
            </ul>
          </div>
          <div class="mt-6 rounded-2xl border border-blue-200 bg-gradient-to-r from-blue-500/10 via-indigo-500/10 to-purple-500/10 p-4 text-center text-xs font-medium uppercase tracking-wide text-blue-600">Layout hiện đại</div>
        </label>

        <label class="group flex h-full cursor-pointer flex-col justify-between rounded-3xl border-2 bg-white p-6 transition-all duration-300 hover:border-amber-400 hover:shadow-lg <%= 'border-amber-500 shadow-lg' if template_three_selected %>">
          <div class="space-y-5">
            <div class="flex items-center justify-between">
              <span class="inline-flex items-center gap-2 text-base font-semibold text-slate-900">
                <span class="inline-flex h-10 w-10 items-center justify-center rounded-2xl bg-amber-100 text-amber-600">L</span>
                Luxury template
              </span>
              <%= form.radio_button :homepage_variant, :template_3, class: "h-4 w-4 border-slate-300 text-amber-600 focus:ring-amber-500" %>
            </div>
            <p class="text-sm text-slate-500">Giao diện tối giản mang phong cách lifestyle cao cấp với hero toàn màn hình và showcase bất đối xứng.</p>
            <ul class="space-y-2 text-sm text-slate-600">
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-amber-100 text-xs font-semibold text-amber-600">1</span> Hiệu ứng parallax tinh tế và typography sang trọng.</li>
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-amber-100 text-xs font-semibold text-amber-600">2</span> Lưới sản phẩm dạng masonry, testimonials và newsletter đồng bộ.</li>
              <li class="flex items-center gap-2"><span class="inline-flex h-5 w-5 items-center justify-center rounded-full bg-amber-100 text-xs font-semibold text-amber-600">3</span> Footer tối màu cao cấp với form đăng ký nhanh.</li>
            </ul>
          </div>
          <div class="mt-6 rounded-2xl border border-amber-200 bg-gradient-to-r from-amber-500/10 via-amber-500/5 to-amber-500/10 p-4 text-center text-xs font-medium uppercase tracking-wide text-amber-600">Layout sang trọng</div>
        </label>
      </div>

      <div class="flex flex-col gap-4 border-t border-slate-200 pt-6 sm:flex-row sm:items-center sm:justify-between">
        <p class="text-sm text-slate-500">Thay đổi sẽ có hiệu lực ngay lập tức trên trang chủ công khai.</p>
        <div class="flex gap-3">
          <%= link_to "Xem trước", root_path, class: "inline-flex items-center gap-2 rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-600 transition hover:border-indigo-300 hover:text-indigo-600" %>
          <%= form.submit "Lưu cài đặt", class: "inline-flex items-center gap-2 rounded-full bg-indigo-600 px-6 py-2.5 text-sm font-semibold text-white transition hover:bg-indigo-500" %>
        </div>
      </div>
    <% end %>
  </section>
</div>
