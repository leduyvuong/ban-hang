<div class="grid gap-6 lg:grid-cols-12">
  <section class="lg:col-span-8 space-y-6">
    <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
      <h2 class="text-lg font-semibold text-slate-900">Overview</h2>
      <p class="mt-1 text-sm text-slate-500">Quick metrics to keep track of your store.</p>
      <dl class="mt-6 grid gap-4 sm:grid-cols-3">
        <div class="rounded-2xl border border-indigo-100 bg-indigo-50/50 px-4 py-5 shadow-sm">
          <dt class="text-xs font-semibold uppercase tracking-wide text-indigo-600">Products</dt>
          <dd class="mt-2 text-3xl font-semibold text-slate-900"><%= @products_count %></dd>
          <dd class="mt-1 text-xs text-slate-500">Total products live in store</dd>
        </div>
        <div class="rounded-2xl border border-emerald-100 bg-emerald-50/50 px-4 py-5 shadow-sm">
          <dt class="text-xs font-semibold uppercase tracking-wide text-emerald-600">Orders</dt>
          <dd class="mt-2 text-3xl font-semibold text-slate-900"><%= @orders_count || 0 %></dd>
          <dd class="mt-1 text-xs text-slate-500">Orders placed in the system</dd>
        </div>
        <div class="rounded-2xl border border-slate-200 bg-white px-4 py-5 shadow-sm">
          <dt class="text-xs font-semibold uppercase tracking-wide text-slate-500">Pending tasks</dt>
          <dd class="mt-2 text-3xl font-semibold text-slate-900"><%= [@products_count.zero? ? 1 : 0, (@orders_count || 0).zero? ? 1 : 0].sum %></dd>
          <dd class="mt-1 text-xs text-slate-500">Suggested setup tasks</dd>
        </div>
      </dl>
    </div>

    <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
      <div class="flex items-center justify-between">
        <div>
          <h2 class="text-lg font-semibold text-slate-900">Recent products</h2>
          <p class="text-sm text-slate-500">Newest additions to your catalogue.</p>
        </div>
        <%= link_to "View all", admin_products_path, class: "text-sm font-medium text-indigo-600 hover:text-indigo-500" %>
      </div>
      <% if @recent_products.any? %>
        <ul class="mt-4 divide-y divide-slate-100">
          <% @recent_products.each do |product| %>
            <li class="flex items-center justify-between gap-4 py-3">
              <div>
                <p class="text-sm font-semibold text-slate-900"><%= product.name %></p>
                <p class="text-xs text-slate-500">Added <%= time_ago_in_words(product.created_at) %> ago</p>
              </div>
              <div class="flex items-center gap-2 text-sm text-slate-500">
                <span><%= number_to_currency(product.price) %></span>
                <%= link_to "Edit", edit_admin_product_path(product), class: "text-indigo-600 hover:text-indigo-500" %>
              </div>
            </li>
          <% end %>
        </ul>
      <% else %>
        <div class="mt-4 rounded-2xl border border-dashed border-slate-200 bg-slate-50 px-4 py-6 text-center text-sm text-slate-500">
          No products yet. Start by creating your first product.
        </div>
      <% end %>
    </div>
  </section>

  <aside class="space-y-6 lg:col-span-4">
    <div class="rounded-3xl border border-indigo-100 bg-indigo-50/60 p-6 shadow-sm">
      <h2 class="text-lg font-semibold text-indigo-700">Welcome to BanHang admin</h2>
      <p class="mt-1 text-sm text-indigo-600">Keep your inventory updated and respond to orders quickly to maintain customer trust.</p>
      <div class="mt-4 space-y-2">
        <%= link_to "Create product", new_admin_product_path, class: "inline-flex w-full items-center justify-center rounded-full bg-indigo-600 px-4 py-2 text-sm font-semibold text-white transition hover:bg-indigo-500" %>
        <%= link_to "Review orders", admin_orders_path, class: "inline-flex w-full items-center justify-center rounded-full border border-indigo-200 px-4 py-2 text-sm font-semibold text-indigo-600 transition hover:border-indigo-300" %>
      </div>
    </div>

    <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
      <h2 class="text-lg font-semibold text-slate-900">Shortcuts</h2>
      <ul class="mt-3 space-y-2 text-sm text-indigo-600">
        <li><%= link_to "View store homepage", root_path, class: "flex items-center gap-2 hover:text-indigo-500" %></li>
        <li><%= link_to "Manage assets", "#", class: "flex items-center gap-2 hover:text-indigo-500" %></li>
        <li><%= link_to "Invite teammate", "#", class: "flex items-center gap-2 hover:text-indigo-500" %></li>
      </ul>
    </div>
  </aside>
</div>
