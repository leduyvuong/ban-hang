<%= form_with model: @page_layout, url: admin_page_layout_path, method: :put, data: { controller: "layout-builder", action: "turbo:submit-start->layout-builder#beforeSubmit" } do |form| %>
  <%= form.hidden_field :layout_config, value: @page_layout.layout_config.to_json, data: { layout_builder_target: "input" } %>

  <div class="grid gap-6 lg:grid-cols-[320px,1fr]">
    <aside class="hidden rounded-3xl border border-slate-200 bg-white p-4 shadow-sm dark:border-slate-700 dark:bg-slate-900 lg:block" data-layout-builder-target="sidebar">
      <h2 class="text-lg font-semibold text-slate-900 dark:text-slate-100">Thư viện component</h2>
      <p class="mt-1 text-sm text-slate-500 dark:text-slate-300">Kéo thả vào canvas để thêm vào trang.</p>
      <ul class="mt-4 space-y-3">
        <% @components_catalog.each do |component| %>
          <li>
            <button
              type="button"
              class="flex w-full items-start gap-3 rounded-2xl border border-slate-200 bg-slate-50 p-3 text-left text-sm text-slate-700 shadow-sm transition hover:border-blue-400 hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-500 dark:border-slate-600 dark:bg-slate-800 dark:text-slate-200"
              draggable="true"
              data-component-type="<%= component.type %>"
              data-component-default-config="<%= json_escape(component.default_config.to_json) %>"
              data-action="dragstart->layout-builder#startSidebarDrag dragend->layout-builder#endSidebarDrag"
            >
              <span class="mt-1 inline-flex h-8 w-8 flex-none items-center justify-center rounded-full bg-blue-100 text-blue-600">+</span>
              <span>
                <span class="font-semibold"><%= component.label %></span>
                <span class="mt-1 block text-xs text-slate-500 dark:text-slate-400"><%= component.description %></span>
              </span>
            </button>
          </li>
        <% end %>
      </ul>
    </aside>

    <div class="space-y-6">
      <div class="flex items-center justify-between gap-4">
        <div>
          <h1 class="text-2xl font-semibold text-slate-900 dark:text-slate-100">Canvas trang chủ</h1>
          <p class="text-sm text-slate-500 dark:text-slate-300">Sắp xếp các component, chỉnh sửa nội dung và xuất bản.</p>
        </div>
        <div class="flex gap-2">
          <button type="submit" class="inline-flex items-center rounded-full border border-slate-300 px-4 py-2 text-sm font-medium text-slate-700 transition hover:border-blue-400 hover:text-blue-600 dark:border-slate-600 dark:text-slate-200" data-action="click->layout-builder#saveDraft">
            Lưu nháp
          </button>
          <button type="submit" name="publish" value="1" class="inline-flex items-center rounded-full bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow transition hover:bg-blue-500" data-action="click->layout-builder#publish">
            Xuất bản
          </button>
        </div>
      </div>

      <div class="rounded-3xl border border-slate-200 bg-slate-50/70 p-4 shadow-inner transition dark:border-slate-700 dark:bg-slate-900" data-layout-builder-target="mobileLibrary">
        <details class="lg:hidden">
          <summary class="cursor-pointer text-sm font-semibold text-slate-700 dark:text-slate-200">Danh sách component</summary>
          <div class="mt-4 grid gap-3 sm:grid-cols-2" data-layout-builder-target="mobileComponentList">
            <% @components_catalog.each do |component| %>
              <button
                type="button"
                class="flex w-full items-start gap-3 rounded-2xl border border-slate-200 bg-white p-3 text-left text-sm text-slate-700 shadow-sm transition hover:border-blue-400 hover:bg-blue-50 focus:outline-none focus:ring-2 focus:ring-blue-500 dark:border-slate-600 dark:bg-slate-800 dark:text-slate-200"
                draggable="true"
                data-component-type="<%= component.type %>"
                data-component-default-config="<%= json_escape(component.default_config.to_json) %>"
                data-action="dragstart->layout-builder#startSidebarDrag dragend->layout-builder#endSidebarDrag"
              >
                <span class="mt-1 inline-flex h-8 w-8 flex-none items-center justify-center rounded-full bg-blue-100 text-blue-600">+</span>
                <span>
                  <span class="font-semibold"><%= component.label %></span>
                  <span class="mt-1 block text-xs text-slate-500 dark:text-slate-400"><%= component.description %></span>
                </span>
              </button>
            <% end %>
          </div>
        </details>
      </div>

      <div
        class="relative min-h-[480px] rounded-3xl border border-dashed border-slate-300 bg-[radial-gradient(circle_at_1px_1px,_theme(colors.slate.200)_1px,_transparent_0)] bg-[length:20px_20px] p-6 transition dark:border-slate-600 dark:bg-[radial-gradient(circle_at_1px_1px,_theme(colors.slate.700)_1px,_transparent_0)]"
        data-layout-builder-target="canvas"
        data-action="dragover->layout-builder#allowDrop dragenter->layout-builder#highlightDropZone dragleave->layout-builder#removeDropZoneHighlight drop->layout-builder#handleDrop"
      >
        <div class="space-y-4" data-layout-builder-target="emptyState" <%= "hidden" if @page_layout.components.any? %>>
          <div class="flex flex-col items-center justify-center gap-2 rounded-3xl border border-dashed border-slate-300 bg-white/60 p-10 text-center text-slate-500 dark:border-slate-600 dark:bg-slate-800/70 dark:text-slate-300">
            <span class="text-2xl">🧩</span>
            <p class="text-sm">Kéo thả component từ thư viện để bắt đầu xây dựng trang chủ.</p>
          </div>
        </div>

        <div class="flex flex-col gap-5" data-layout-builder-target="componentList">
          <% @page_layout.ordered_components.each do |component_config| %>
            <%= render "admin/page_layouts/component", component_config: component_config %>
          <% end %>
        </div>
      </div>

      <% if @published_page.present? %>
        <div class="rounded-3xl border border-slate-200 bg-white p-5 text-sm text-slate-500 shadow-sm dark:border-slate-700 dark:bg-slate-900 dark:text-slate-300">
          <p>Đã xuất bản lần cuối: <strong><%= l(@published_page.published_at, format: :long) %></strong></p>
        </div>
      <% end %>
    </div>
  </div>

  <% @components_catalog.each do |component| %>
    <template data-layout-builder-target="template" data-component-type="<%= component.type %>" data-component-default-config="<%= json_escape(component.default_config.to_json) %>">
      <%= render "admin/page_layouts/component", component_config: { "id" => "__COMPONENT_ID__", "type" => component.type, "order" => 0, "config" => component.default_config.deep_dup } %>
    </template>
  <% end %>
<% end %>
