<div class="grid gap-4 sm:grid-cols-2">
  <label class="flex flex-col text-xs font-medium text-slate-500">
    Màu nền
    <input type="color" value="<%= config["background_color"] %>" class="mt-1 h-10 w-full rounded-lg border border-slate-200" data-action="input->layout-builder#updateField" data-field-name="background_color" />
  </label>
  <label class="flex flex-col text-xs font-medium text-slate-500">
    Khoảng cách giữa cột
    <select class="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2 text-sm text-slate-900 shadow-sm focus:border-blue-500 focus:outline-none" data-action="change->layout-builder#updateField" data-field-name="gap">
      <option value="sm" <%= 'selected' if config["gap"] == 'sm' %>>Nhỏ</option>
      <option value="md" <%= 'selected' if config["gap"] == 'md' %>>Trung bình</option>
      <option value="lg" <%= 'selected' if config["gap"].blank? || config["gap"] == 'lg' %>>Rộng</option>
    </select>
  </label>
</div>
<p class="text-xs text-slate-500 dark:text-slate-400">
  Kéo các component khác vào từng cột bên dưới để hoàn thiện bố cục.
</p>
