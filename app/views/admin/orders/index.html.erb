<div class="space-y-6">
  <div class="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm">
    <div class="flex flex-wrap items-center justify-between gap-4">
      <div>
        <h2 class="text-lg font-semibold text-slate-900">Order history</h2>
        <p class="text-sm text-slate-500">Monitor fulfilment progress and payment status.</p>
      </div>
      <%= link_to "Export CSV", "#", class: "inline-flex items-center gap-2 rounded-full border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-600 transition hover:border-indigo-200 hover:text-indigo-600" %>
    </div>

    <% if @orders.any? %>
      <div class="mt-6 overflow-hidden rounded-2xl border border-slate-200">
        <table class="min-w-full divide-y divide-slate-200">
          <thead class="bg-slate-50">
            <tr class="text-xs uppercase tracking-wide text-slate-500">
              <th scope="col" class="px-4 py-3 text-left">Order</th>
              <th scope="col" class="px-4 py-3 text-left">Status</th>
              <th scope="col" class="px-4 py-3 text-left">Placed</th>
              <th scope="col" class="px-4 py-3 text-right">Total</th>
              <th scope="col" class="px-4 py-3 text-right">Actions</th>
            </tr>
          </thead>
          <tbody class="divide-y divide-slate-100 bg-white text-sm text-slate-600">
            <% @orders.each do |order| %>
              <tr class="hover:bg-indigo-50/40">
                <td class="px-4 py-3">
                  <p class="font-semibold text-slate-900">#<%= order.order_number %></p>
                  <p class="text-xs text-slate-500">Created <%= time_ago_in_words(order.created_at || order.placed_at || Time.current) %> ago</p>
                </td>
                <td class="px-4 py-3 align-middle">
                  <span class="inline-flex items-center rounded-full bg-slate-100 px-3 py-1 text-xs font-semibold capitalize text-slate-600"><%= order.status %></span>
                </td>
                <td class="px-4 py-3 align-middle text-xs text-slate-500"><%= (order.placed_at || order.created_at)&.strftime("%b %-d, %Y %H:%M") || "Pending" %></td>
                <td class="px-4 py-3 align-middle text-right font-semibold text-slate-900"><%= number_to_currency(order.total) %></td>
                <td class="px-4 py-3 align-middle text-right">
                  <%= link_to "View", admin_order_path(order), class: "inline-flex items-center rounded-full border border-indigo-200 px-3 py-1 text-xs font-semibold text-indigo-600 hover:bg-indigo-50" %>
                </td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    <% else %>
      <div class="mt-6 rounded-2xl border border-dashed border-slate-200 bg-slate-50 px-4 py-10 text-center text-sm text-slate-500">
        No orders yet. They'll appear here once customers start checking out.
      </div>
    <% end %>
  </div>
</div>
